#!/bin/sh

set -e

ONEHOME=/var/lib/one
ONE_GROUP=oneadmin
ONE_USER=oneadmin

if [ "$1" = "remove" ]; then
    # Remove logs
    rm -rf /var/log/one/*
    systemctl daemon-reload >/dev/null || true
fi

#DEBHELPER#
